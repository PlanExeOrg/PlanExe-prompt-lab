# Code Review (codex)

## Bugs Found

- **B1 — Follow-up call failures are silently downgraded to successful plan runs.** In the live source, `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:259` catches any exception from a later LLM call, and `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:272` only re-raises when zero prior calls succeeded. Otherwise `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:276` logs and returns partial results. The runner then records the plan as a full success at `/Users/neoneye/git/PlanExeGroup/PlanExe/prompt_optimizer/runner.py:302`, writes it as `status="ok"` at `/Users/neoneye/git/PlanExeGroup/PlanExe/prompt_optimizer/runner.py:311`, and later treats that incomplete output as resume-safe at `/Users/neoneye/git/PlanExeGroup/PlanExe/prompt_optimizer/runner.py:342`. This exactly explains why raw artifacts can contain only 2 responses while `events.jsonl` shows no `run_single_plan_error`.

- **B2 — The optimizer and the production pipeline are evaluating different system prompts.** The optimizer always injects an external prompt file via `/Users/neoneye/git/PlanExeGroup/PlanExe/prompt_optimizer/runner.py:114` and `/Users/neoneye/git/PlanExeGroup/PlanExe/prompt_optimizer/runner.py:436`, but the production task calls `IdentifyPotentialLevers.execute(...)` with no `system_prompt` override at `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/plan/run_plan_pipeline.py:386`. That falls back to the checked-in constant at `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:154` and `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:214`. As a result, prompt-lab runs can look fixed while the shipped pipeline still uses older instructions.

- **B3 — The production default prompt still bakes in the exact quality regressions the insights call out.** The checked-in schema and prompt explicitly require quantified consequences at `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:36`, `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:39`, `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:163`, and `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:164`; force the review field into English `Controls ...` / `Weakness:` markers at `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:53`, `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:95`, and `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:97`; and push a conservative→moderate→radical triad plus an emerging-tech radical option at `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:169` and `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:193`. Because `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/plan/run_plan_pipeline.py:386` uses that default prompt, fabricated percentages, rigid review grammar, and tech-forcing are not just model noise; they are encouraged by the live code path.

## Suspect Patterns

- **S1 — The PR-stack review validator swung from too strict to almost structure-free.** In the PR worktree, `/Users/neoneye/git/PlanExeGroup/PlanExe/.claude/worktrees/silly-purring-lovelace/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:130` through `/Users/neoneye/git/PlanExeGroup/PlanExe/.claude/worktrees/silly-purring-lovelace/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:142` only enforce minimum length and “no square brackets.” That fixes the English-only bug, but it also accepts any long, non-placeholder text, so low-information review sentences can now pass unchecked. This likely explains why non-`Controls` reviews persist after the validator relaxation, but whether that is acceptable depends on downstream consumers.

- **S2 — `OPTIMIZE_INSTRUCTIONS` is documentation-only, not a runtime source of truth.** The PR worktree defines it at `/Users/neoneye/git/PlanExeGroup/PlanExe/.claude/worktrees/silly-purring-lovelace/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:27`, but there are no code references to that constant in the runner or execution path. That means the anti-fabrication, anti-hype, and i18n rules can be present in comments and analysis guidance while the actual runtime prompt drifts away.

## Improvement Opportunities

- **I1 — Make partial completion explicit instead of silently calling it `ok`.** Add `expected_calls`, `completed_calls`, and failed-call metadata to the raw output, and emit a separate partial status from `/Users/neoneye/git/PlanExeGroup/PlanExe/prompt_optimizer/runner.py:302` instead of treating any returned lever list as a clean success. The current exception handling at `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:259` and `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:276` makes debugging missing calls unnecessarily hard.

- **I2 — Add a post-generation quality gate for unsupported quantification, hype language, and option completeness.** Both the main file and the PR worktree validate only option count and review shape at `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:73`, `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:86`, `/Users/neoneye/git/PlanExeGroup/PlanExe/.claude/worktrees/silly-purring-lovelace/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:115`, and `/Users/neoneye/git/PlanExeGroup/PlanExe/.claude/worktrees/silly-purring-lovelace/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:128`. There is still no code-level backstop for `%` claims, date-like effort claims, banned hype words, or label-only options if prompt wording regresses again.

- **I3 — Unify the prompt source of truth across optimizer and production.** Either load the registered prompt file in production too, or generate both the runtime constant and prompt-lab prompt from the same template. The current split between `/Users/neoneye/git/PlanExeGroup/PlanExe/prompt_optimizer/runner.py:114`, `/Users/neoneye/git/PlanExeGroup/PlanExe/prompt_optimizer/runner.py:436`, `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:154`, and `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/plan/run_plan_pipeline.py:386` is the root cause of the code-vs-experiment mismatch.

- **I4 — Tighten review validation in a language-neutral way instead of relying on either English markers or a 20-character minimum.** The main validator at `/Users/neoneye/git/PlanExeGroup/PlanExe/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:95` is too strict for multilingual prompts, while the PR-stack validator at `/Users/neoneye/git/PlanExeGroup/PlanExe/.claude/worktrees/silly-purring-lovelace/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:139` is too lax. A better middle ground would be “two sentences, no placeholders, second sentence contains critique-like content, and total length over a modest threshold.”

- **I5 — If the call-2/3 reminder stays, pair it with the adjacent credibility reminder.** The PR correctly reinforces sentence completeness at `/Users/neoneye/git/PlanExeGroup/PlanExe/.claude/worktrees/silly-purring-lovelace/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:272` through `/Users/neoneye/git/PlanExeGroup/PlanExe/.claude/worktrees/silly-purring-lovelace/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:274`, but it does not restate the equally brittle “no fabricated numbers / no hype language” rule in the same late-call location where attention is already decaying.

## Trace to Insight Findings

- **B1** explains the missing-call anomaly in `insight_codex.md`: raw outputs with only 2 responses and no corresponding `LLMChatError` or `run_single_plan_error` are exactly what the partial-success path produces.

- **B2** explains the source-state contradiction raised in `insight_claude.md` question 5. The experiments can pass non-`Controls` reviews because the optimizer is feeding a different prompt/validator stack than the production default shown in the checked-in file.

- **B3** maps directly to `insight_claude.md` N2, N4, N5, and N7, plus the unsupported-quantification pattern in `insight_codex.md`. The live default prompt explicitly asks for measurable indicators and English `Controls`/`Weakness` grammar, so the code itself is pushing models toward fabricated numbers and brittle formatting.

- **S1** explains why current runs now accept varied review openings without schema failures. The validator relaxation removed the old `Controls` gate, but it did so by nearly eliminating semantic checks.

- **I2** targets the remaining quality gaps both insight files highlight: shallow later-call options, unsupported percentages, and marketing-language leakage are currently prompt-only constraints with no code enforcement.

## PR Review

- **Implementation vs intent:** The PR commit itself is extremely narrow and matches its stated intent. In `/Users/neoneye/git/PlanExeGroup/PlanExe/.claude/worktrees/silly-purring-lovelace/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:272` through `/Users/neoneye/git/PlanExeGroup/PlanExe/.claude/worktrees/silly-purring-lovelace/worker_plan/worker_plan_internal/lever/identify_potential_levers.py:274`, it inserts the reminder exactly where the blacklist expansion had been diluting the earlier option-quality contract. I do not see a prompt-order bug or duplicated-message bug in this change.

- **Does it address the claimed defect?** Yes. The reminder is placed in the right spot in the follow-up-call prompt assembly and is consistent with the observed elimination of label-only later-call options.

- **Gaps in the PR itself:** The PR is prompt-only. It does not add any validator or post-processing fallback if a model ignores the reminder, and it does not address the neighboring credibility failures that still show up in later calls. It also leaves **B1** untouched, so a later-call failure can still disappear into an `ok` plan result with fewer than 3 raw responses.

- **Risk assessment:** Low risk. The code change is tiny and localized. The real risk is not regression from this diff; it is that the system still relies on prompt wording alone for option quality and credibility.

## Summary

- The main confirmed code bug is the silent partial-success path: later-call failures become `ok` plans, which matches the “2 raw calls, no error event” artifacts.
- The next major issue is prompt split-brain: prompt-lab evaluates one system prompt while the production pipeline uses another.
- The checked-in production prompt still contains the exact instructions that encourage fabricated quantification, English-only review grammar, and forced radical-tech framing.
- PR #309 itself is a good narrow fix for the targeted llama later-call label collapse, but it does not solve the deeper observability and prompt-alignment problems.
